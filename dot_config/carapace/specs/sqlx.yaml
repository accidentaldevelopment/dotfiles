# yaml-language-server: $schema=https://carapace.sh/schemas/command.json
name: sqlx
description: sqlx utility for managing databases, migrations, and enabling "offline" mode.
parsing: disabled
completion:
  positionalany: ["$carapace.bridge.CarapaceBin([cargo-sqlx])"]
